{
  "name": "viewer.gg",
  "private": true,
  "version": "1.0.0",
  "description": "Root workspace - Main Next.js app is in the ./web directory",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start",
    "lint": "cd web && npm run lint",
    "discord-bot": "node --loader ts-node/esm discord-bot/index.ts"
  },
  "dependencies": {
    "discord.js": "^14.14.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "dotenv": "^16.4.4",
    "ts-node": "^10.9.2",
    "typescript": "~5.8.2"
  }
}
